#@ load("@ytt:data", "data")
#@ load("@ytt:assert", "assert")
#@ load("@ytt:yaml", "yaml")
#@ load("@ytt:json", "json")

#@ def get_arrays(application):
#@ if application == "vxflexos":
#@    return get_arrays_vxflexos()
#@ elif application == "powerstore":
#@    return get_arrays_powerstore()
#@ assert.fail("containers for application {} not found".format(application))
#@ end
#@ end

#! Storage Arrays for application csi-vxflexos
#@ def get_arrays_vxflexos():
#@ return json.encode(data.values.arrays)
#@ end

#! Storage Arrays for application csi-powerstore
#@ def get_arrays_powerstore():
#@ return yaml.encode(data.values.config)
#@ end

